# Lint Rules

- `wiki/index.md` and `wiki/log.md` may omit YAML frontmatter.
- Other Markdown files in `wiki/` must start with YAML frontmatter.
- Obsidian wikilinks should point to existing Markdown pages or existing local assets.
- Keep generated summaries reviewable and cite the source path when practical.
